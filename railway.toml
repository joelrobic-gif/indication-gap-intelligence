[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "PORT=${PORT:-3000} npm start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
